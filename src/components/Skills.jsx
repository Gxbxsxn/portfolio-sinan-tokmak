// Archivo no usado en la versión actual del portafolio (se conserva porque
// el sistema de archivos no permite eliminarlo). Puedes borrarlo tú mismo
// sin problema: no se importa desde ningún otro archivo.
export default function Deprecated() {
  return null
}
